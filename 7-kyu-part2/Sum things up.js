/*
Description:
Create code inside the function sum(array) block, so that the elements of the array add up.

For example, if your array = [23, 32, -8], then adding these three elements would equal 47.
*/
function sum(array) {
 return array.reduce((a,b)=>a+b,0) 
}
